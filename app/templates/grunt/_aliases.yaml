default:
    - clean
    - 'exec:bower'
    - jshint
    - sass
    - postcss
    - copy<% if (includeModernizr) { %>
    - 'modernizr:dev'<% } %>
    - merge-json
    - patternlab

serve:
    - default
    - browserSync
    - watch

prepare:
    - default<% if (includeModernizr) { %>
    - 'modernizr:prepare'<% } %>

'build-patternlab':
    - patternlab
