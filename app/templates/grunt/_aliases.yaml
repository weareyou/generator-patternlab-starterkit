default:
    - clean
    - 'exec:bower'
    - jshint
    - sass
    - postcss
    - copy<% if (includeModernizr) { %>
    - 'modernizr:dev'
    - 'modernizr:prepare'<% } %>
    - merge-json
    - patternlab

serve:
    - default
    - browserSync
    - watch

prepare:
    - default

'build-patternlab':
    - patternlab
