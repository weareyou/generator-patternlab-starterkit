default:
    - clean
    - 'exec:bower'
    - jshint
    - sass
    - postcss
    - copy<% if (includeModernizr) { %>
    - modernizr<% } %>
    - merge-json
    - patternlab

serve:
    - default
    - browserSync
    - watch

'build-patternlab':
    - patternlab
