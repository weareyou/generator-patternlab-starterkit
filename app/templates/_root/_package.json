{
  "name": "<%= _.kebabCase(answers.projectName) %>",
  "version": "0.0.0",
  "description": "<%= answers.projectName %> project files",
  "scripts": {
    "serve": "gulp patternlab:serve",
    "watch": "gulp patternlab:watch",
    "prepare": "gulp patternlab:build"
  },
  "dependencies": {
    "browser-sync": "^2.0.0",
    "gulp": "gulpjs/gulp#4.0",
    "minimist": "^1.2.0",
    "patternlab-node": "^2.0.0",
    "styleguidekit-assets-default": "^3.0.0",
    "styleguidekit-mustache-default": "^3.0.0"<% if (answers.starterkit === 'starterkit-mustache-demo') { %>,
    "starterkit-mustache-demo": "^4.0.0"<% } else if (answers.starterkit === 'starterkit-colours-mustache') { %>,
    "starterkit-colours-mustache": "^1.0.0"<% } %>
  }
}
